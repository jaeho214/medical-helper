spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/medical_helper?verifyServerCertificate=false&useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: medicalhelper
    password : 1q2w3e


  jpa:
    hibernate:
      ddl-auto : update
    generate-ddl : true
    show-sql : true
    database : mysql
    database-platform : org.hibernate.dialect.MySQL5InnoDBDialect

    servlet:
      multipart:
        enabled: true
        max-file-size: 1MB
        max-request-size: 1MB


resources:
  image-locations: C:\artSharing\
  image-url: 127.0.0.1:8080